{
  "name": "captureme-android",
  "version": "1.0.0",
  "description": "captureME Android Screen Recorder",
  "main": "index.html",
  "scripts": {
    "build": "npx @capacitor/cli copy android",
    "cap:add": "npx @capacitor/cli add android",
    "cap:sync": "npx @capacitor/cli sync android"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
